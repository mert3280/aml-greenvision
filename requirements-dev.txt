# GreenVision development / test dependencies.
# Install on top of requirements.txt:  pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient (WS7 tests)

# Formatting / linting.
black>=24.0
isort>=5.13
flake8>=7.0

# Demo dashboard (WS8).
streamlit>=1.39
