# GreenVision runtime dependencies.
#
# PyTorch is pinned to the CUDA 12.6 build already present in venv/.
# DO NOT bump torch/torchvision (agent.md guardrail #6) without explicit confirmation.
# CUDA wheels are served from PyTorch's own index. CPU-only machines can swap the line
# below to:  --extra-index-url https://download.pytorch.org/whl/cpu  and drop the +cu126.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.0+cu126
torchvision==0.27.0+cu126

# Numerics / imaging (already installed; pinned to the venv versions).
numpy==2.4.4
pillow==12.2.0

# Data split + metrics (WS2/WS6).
scikit-learn>=1.5
matplotlib>=3.9

# Experiment tracking (WS5).
mlflow>=2.18

# Serving (WS7 — installed now so the env is complete).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-multipart>=0.0.12
